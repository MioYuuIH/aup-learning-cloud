# Team Resource Mapping Configuration
# Maps GitHub teams (or user groups) to available resource types.

mapping:
  # CPU-only access
  cpu:
    - cpu

  # GPU access (all GPU courses)
  gpu:
    - Course-CV
    - Course-DL
    - Course-LLM
    - Course-PhySim

  # Official team - full access
  official:
    - cpu
    - Course-CV
    - Course-DL
    - Course-LLM
    - Course-PhySim

  # AUP program participants
  AUP:
    - Course-CV
    - Course-DL
    - Course-LLM
    - Course-PhySim

  # Native (local) users default
  native-users:
    - Course-CV
    - Course-DL
    - Course-LLM
